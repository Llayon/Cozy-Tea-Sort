# Cozy Tea Sort — no server-side environment variables required.
# The game is a fully client-side Telegram Mini App (React + Pixi.js v8).
# Telegram WebApp bootstrap is loaded via CDN in index.html.
